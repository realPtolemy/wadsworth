[project]
name = "wadsworth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mujoco>=3.6.0",
    "numpy>=2.4",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
